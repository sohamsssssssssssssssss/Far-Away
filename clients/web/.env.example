# Base URL of the DisasterMind backend API.
# Defaults to the deployed Railway instance if unset; override for local dev.
VITE_API_BASE_URL=http://localhost:8000

# Optional: Anthropic API key for the post-incident report generator
# (clients/web report module). Leave unset to use the mock/offline path.
VITE_ANTHROPIC_API_KEY=
